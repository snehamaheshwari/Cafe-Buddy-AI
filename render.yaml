services:
  - type: web
    name: cafe-buddy
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      # Optional: set in Render dashboard to enable full Claude AI in chatbot
      # Leave blank → Smart Analytics Engine works without any key
      - key: ANTHROPIC_API_KEY
        sync: false
